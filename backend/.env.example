# Directory used for uploaded datasets, sqlite db, and geo cache
SENTINEL_DATA_DIR=/tmp/sentinel

# Comma-separated list of allowed frontend origins, or *
# Example: http://localhost:5173,https://your-frontend-domain.com
CORS_ALLOW_ORIGINS=*

# Optional runtime host/port for backend/run.py
HOST=0.0.0.0
PORT=8000
